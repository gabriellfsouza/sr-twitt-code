// const twitts = require('../localdb/twitts.json');

class Twitt {
  constructor() {

  }

  /**
   * @param {Number} a
   * @param {Number} b
   */
  sum(a, b) {
    return a + b;
  }
}

module.exports = Twitt;
